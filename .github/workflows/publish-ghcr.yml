name: Docker Image CI for GHCR

on:
    push

jobs:
    build_and_publish:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: npm install
              run: npm ci
            - name: run the unit tests
              run: npm test
            - name: Publish the image to github container registry
              run: |
                docker login --username tipb47 --password ${{secrets.ACCESS_TOKEN}} ghcr.io
                docker build -t ghcr.io/tipb47/arithmetic-service:1 --file Dockerfile .  
                docker push ghcr.io/tipb47/arithmetic-service:1 
            - name: Publish the image to azure container registry
              run: |
                docker login --username practicum16tip --password ${{secrets.AZURE_CONTAINER_REGISTRY}} practicum16tip.azurecr.io
                docker tag ghcr.io/tipb47/arithmetic-service:1   practicum16tip.azurecr.io/arithmetic-service:1
                docker push practicum16tip.azurecr.io/arithmetic-service:1

